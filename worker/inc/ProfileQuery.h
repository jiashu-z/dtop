#ifndef DTOP_RPC_PROFILEQUERY_H
#define DTOP_RPC_PROFILEQUERY_H

namespace dtop {
namespace worker {

	enum ProfileType {
			CPU_USAGE, MEM_USAGE
	};

	class ProfileQuery {

	};
}
}

#endif //DTOP_RPC_PROFILEQUERY_H
